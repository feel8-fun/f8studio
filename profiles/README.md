# Profiles

- Canonical profile schema in `schema/profile.schema.json`.
- Examples in `examples/` for web-only vs web-plus-daemon modes.
- Use schema validation in tests/contract to ensure published profiles stay compatible.
