# Shared

- Common models/types and codegen outputs (TS/C++) derived from `api/specs/*`.
- Provide thin wrappers to map envelope/status/error to idiomatic exceptions/results.
- Keep generated code segregated from handwritten helpers to allow regeneration.
