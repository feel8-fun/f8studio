[project]
name = "f8pysdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datamodel-code-generator[http]>=0.50.0",
]


[tool.uv]
package = true
