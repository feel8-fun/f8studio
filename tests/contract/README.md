# Contract Tests

- Generated clients hit local NATS (in-memory) against daemon handlers.
- Validate envelope, error model, and subject bindings from `api/specs`.
- Keep gold fixtures for profile examples and config apply responses.
