schemaVersion: f8serviceEntry/1
serviceClass: f8.audiocap
label: Audio Capture
version: 0.0.1
launch:
  command: powershell.exe
  args: ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", "scripts/run_audiocap.ps1"]
  env: {}
  workdir: "../../../"

